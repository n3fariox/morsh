[package]
name = "morsh-proto"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Protocol Buffer definitions for the Morsh protocol"

[dependencies]
prost = "0.14"

[build-dependencies]
prost-build = "0.14"
