[package]
name = "morsh-terminal"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Terminal emulation adapter for morsh using libghostty-vt"

[dependencies]
libghostty-vt = "0.2"

[build-dependencies]
