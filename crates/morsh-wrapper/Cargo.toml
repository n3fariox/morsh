[package]
name = "morsh-wrapper"
version = "0.1.0"
edition = "2021"
description = "Morsh wrapper binary (replaces mosh.pl) - SSH launch + connection"

[[bin]]
name = "morsh"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
