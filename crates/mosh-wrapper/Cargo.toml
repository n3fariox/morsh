[package]
name = "mosh-wrapper"
version = "0.1.0"
edition = "2021"
description = "Mosh wrapper binary (replaces mosh.pl) - SSH launch + connection"

[[bin]]
name = "mosh"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
log = "0.4"
env_logger = "0.11"
